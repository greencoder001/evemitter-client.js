{
  "name": "evemitter-client.js",
  "version": "1.0.0",
  "description": "Evemitter CLient for JavaScript",
  "main": "dist/bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && npm run config",
    "config": "python setup.py",
    "build": "babel src -d dist && uglifyjs dist/main.js -o dist/bundle.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greencoder001/evemitter-client.js.git"
  },
  "author": "Green_Lab",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/greencoder001/evemitter-client.js/issues"
  },
  "homepage": "https://github.com/greencoder001/evemitter-client.js#readme",
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "uglify-js": "^3.10.4"
  }
}